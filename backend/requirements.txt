# ── API / web framework ───────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic[email]==2.10.3
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
PyJWT==2.10.1
slowapi==0.1.9
structlog==24.4.0

# ── Database ──────────────────────────────────────────────────────────────
supabase==2.10.0

# ── Encryption / HTTP ─────────────────────────────────────────────────────
cryptography==43.0.3
httpx>=0.26,<0.28

# ── Ingestion ─────────────────────────────────────────────────────────────
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0
python-dotenv>=1.0,<2          # pipeline.py load_dotenv()

# ── Frontend (also needed when running tests that import app.py) ──────────
streamlit==1.41.0
streamlit-echarts==0.4.0
pandas>=2.0,<3
numpy>=1.24,<2
python-dateutil>=2.8,<3
plotly==5.24.1

# ── Security scanning (CI only) ───────────────────────────────────────────
bandit==1.8.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
